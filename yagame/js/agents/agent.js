class Agent {
	constructor(config, state) {
		this.config = config;
		this.state = state;
	}
	input(message, user) { }
	trigger(outputConfig) { }

	output(message, outputConfig) { }
	log(message, outputConfig) { }
}